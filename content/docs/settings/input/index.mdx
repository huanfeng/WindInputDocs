---
title: 输入设置
description: 候选检索范围、按键行为、候选无效按键、默认状态、生僻字、简繁转换、Emoji 与 Unicode 输入——设置工具「输入」页逐项说明
---

import { Since } from "@/components/since";

设置工具的「输入」页管的是**输入过程中的行为**。内容较多的几块拆到了二级页：

<Cards>
  <Card title="标点与符号" href="/docs/settings/input/punctuation" description="字符与标点、智能符号、标点配对三张卡片的完整说明" />
  <Card title="快捷输入" href="/docs/settings/input/quick-input" description="分号触发的计算、日期、拼音子模式与自由字面输入" />
  <Card title="临时模式" href="/docs/settings/input/temp-modes" description="临时拼音、临时英文、网址输入" />
  <Card title="联想" href="/docs/settings/input/association" description="上屏后按刚上屏的内容再给一批候选，分词语联想与智能联想两档" />
</Cards>

页内卡片按「这块管什么」分成几段，越靠前的越是每天都在起作用：

| 段 | 卡片 |
| --- | --- |
| 字符形态 | 字符与标点 / 智能符号 / 标点配对 |
| 按键 | 按键行为 / 候选无效按键 |
| 起始状态 | 默认状态 |
| 临时模式 | 快捷输入 / 临时拼音 / 临时英文 / 生僻字 |
| 特殊来源 | 简繁转换 / Emoji / 网址输入 / Unicode 输入 |
| 候选后处理 | 联想 / 词频 |

本页讲上面四张二级页没覆盖的那些：**候选检索范围**（属「字符与标点」卡）、**按键行为**、**候选无效按键**、**默认状态**、**生僻字**、**简繁转换**、**Emoji**、**Unicode 输入**、**词频**。

对应配置段是 `config.toml` 的 `[input]` 与 `[keys.overflow]`，完整键名与默认值见[输入行为配置](/docs/guides/config/input)。

## 候选检索范围

「字符与标点」卡片的第一项，控制**生僻字要不要出现在候选里**。三档：

| 取值 | 范围 |
| --- | --- |
| 智能（默认） | 按通用规范汉字表过滤，但保留一部分常见但不在表内的字 |
| 常用字 | 只出通用规范汉字表里的字，候选最干净 |
| 全部字符 | 不过滤，GB18030 全集，生僻字全出 |

也可以随时从[功能主菜单](/docs/start/first-input#context-menu)的「检索范围」子菜单切换，不必打开设置。

### 翻到底临时放宽 [#scope-relax]

「智能」档下点右侧的**扩展设置**，里面有两项：

| 选项 | 说明 |
| --- | --- |
| 翻到底临时放宽范围 | 候选翻到最后一页后**再按一次**向后翻页键，本次输入临时显示全部字符，上屏后自动恢复 |
| 放宽候选标记 | 给临时多出来的那些候选加个前缀，与正常候选区分；留空则不加 |

这是为「打一个被同码常用字挡住的生僻字」准备的：不必去设置里切档位、打完再切回来，多按一次翻页键即可。仅「智能」档生效。

<Callout type="info" title="临时拼音也跟着这个设置走">
0.114 起临时拼音已按主输入路径同序接入候选过滤——这里设成什么，临拼就跟着什么。此前临拼完全不过滤，在「智能」档下比主路径多出数百个生僻字。
</Callout>

## 按键行为

四个下拉加一个开关：

| 选项 | 出厂 | 说明 |
| --- | --- | --- |
| 回车键功能 | 上屏编码 | 有编码时按回车怎么处理 |
| 空码时空格键功能 | 上屏编码 | 无候选时按空格怎么处理 |
| 空码时标点键功能 <Since v="0.118" /> | **清空编码** | 无候选时按标点/符号键怎么处理编码 |
| 数字小键盘功能 | 直接输入数字 | 小键盘数字键的行为 |
| 全角时小键盘半角输出 <Since v="0.120" /> | 关 | 打开后，全角状态下小键盘的数字和符号仍出半角，见[下面](#numpad-half-width) |

前三项的取值大体相同，但标点比另两项多一项：

| 取值 | 行为 | 哪几项有 |
| --- | --- | --- |
| 上屏编码 | 把你打的原始编码字母上屏，清空缓冲 | 三项都有 |
| 清空编码 | 直接丢弃缓冲，编码不上屏 | 三项都有 |
| 清空编码，标点也不上屏 <Since v="0.119" /> | 丢弃缓冲，连这个标点本身也不输出——整个按键当没按过 | 只有标点项 |

「空码」就是打了码却一个候选都没出来，多半是打错了字根。这三项管的都是这时候那串废码的去向。

为什么标点要多一项：回车和空格选「清空编码」时，回车不换行、空格也不插空格——这两个键本身就不产出可见字符。标点不一样，选「清空编码」时**标点照常上屏**，因为标点通常正是你想打的那个字符。想连标点一起吞掉（把它当成「取消这串废码」的键），才选第三项。

想让空格也丢弃废码，把「空码时空格键功能」也改成「清空编码」。手机端（Android）的标点项出厂为「上屏编码」。

<Callout type="info" title="0.118 之前这两项列了四个取值">
「上屏并继续输入」「忽略」从未被实现，选了等同「上屏编码」。0.118 起只保留真实存在的取值（当时是两项），旧配置会自动归一为「上屏编码」，行为不变。
</Callout>

数字小键盘：

| 取值 | 行为 |
| --- | --- |
| 直接输入数字 | 小键盘就是打数字，不参与选词 |
| 同主键盘区数字 | 小键盘与主键盘数字键等价，可用于候选选择和快捷输入 |

### 全角时小键盘半角输出 <Since v="0.120" /> [#numpad-half-width]

开着全角时，小键盘打出来的还是 `123`，不是 `１２３`；`. + - * /` 同理。

小键盘的用途就是录数字——金额、编号、日期——全角数字在表格和编号栏里几乎总是错的；而开全角通常只是为了排正文。所以把它单拎出来，不跟主键盘走。

和上面那个「数字小键盘功能」是**两回事**，互不干涉：即使选了「同主键盘区数字」，小键盘照样能选词翻页，只有真正**出字**的那一步才改成半角。

<Callout type="warn" title="只管「按一下直接上屏」的那条路">
临时英文、网址输入、快捷输入这些**先攒进缓冲、整串再上屏**的模式不受它影响——缓冲里只存字符不存来源，`abc12` 里的 `12` 是不是小键盘打的已经分辨不出来了。
</Callout>

## 候选无效按键

这三项回答同一个问题：**你按了一个选词键，但那个位置根本没有候选，该怎么办？**

| 选项 | 触发场景 |
| --- | --- |
| 数字键无效时 | 按的数字超出当前页候选数量（如只有 3 个候选却按了 `7`） |
| 次选三选键无效时 | 候选不足 2 / 3 个却按了次选或三选键 |
| 以词定字键无效时 | 候选词长度不足却按了以词定字键 |

三项共用同一组取值：

| 取值 | 行为 |
| --- | --- |
| 不起作用（默认） | 忽略这次按键，编码与候选都不变 |
| 上屏编码 | 上屏当前的原始编码 |
| 上屏并继续输入 | 上屏编码后保持输入状态 |

<Callout type="info" title="这一组在配置文件里属于 keys 域">
虽然显示在「输入」页，配置键是 `keys.overflow.*`。见[按键配置 · 无效按键策略](/docs/guides/config/keys#无效按键策略keysoverflow)。
</Callout>

## 默认状态

管的是**每次激活输入法时从什么状态开始**。

| 选项 | 说明 | 出厂 |
| --- | --- | --- |
| 中英状态作用域 | 全局统一 / 按应用独立 | 全局统一 |
| 记忆前次状态 | 恢复上次的中英文、全半角和标点状态 | 关 |
| 初始语言模式 | 中文 / 英文 | 中文 |
| 初始字符宽度 | 半角 / 全角 | 半角 |
| 初始标点模式 | 中文标点 / 英文标点 | 中文标点 |

**开启「记忆前次状态」后，下面三个初始项会置灰**——记忆和固定初始值是互斥的两条路。

### 中英状态作用域

| 取值 | 语义 |
| --- | --- |
| 全局统一 | 所有应用共享同一个中英状态。在 A 里切到英文，切到 B 也是英文 |
| 按应用独立 | 每个应用各自记忆中英文。适合「游戏里要保持英文、聊天窗要中文」这类场景 |

按应用独立的记忆**不持久化**，重启输入法后重置。

<Callout type="info" title="与右键菜单「应用独立配置」的关系">
这一项管的是「每个应用各自记住上次状态」；[功能主菜单](/docs/settings/menu#app-config)的「初始输入模式」管的是「切进这个应用时**强制**切到某个状态」。两者可以叠加：先按规则套用初始值，之后在该应用内的手动切换再被独立记忆。
</Callout>

## 生僻字 [#rare-char]

打冷僻汉字用的临时模式：空码时按触发键进入，用**当前方案的编码**打，只出单字。

| 选项 | 说明 |
| --- | --- |
| 生僻字触发键 | 出厂**不绑任何键**，要用先在这里选一个。只能选符号键——字母键请到方案页的「[z 键引导功能](/docs/settings/schema/codetable#z-key-action)」配 |
| 生僻字范围 | 默认只出生僻汉字。emoji、符号这些既不算常用也不算生僻，要它们出现就在这里勾上 |

<Callout type="info" title="不绑触发键就是关闭">
这张卡没有「启用」开关——不绑键就进不去，那已经是关闭状态。
</Callout>

完整说明（进出方式、与检索范围的关系、可纳入的区块）见[生僻字模式](/docs/guides/rare-char-mode)。

## 简繁转换 [#s2t]

一张卡里三行，管的是两个**互斥**的方向（0.121 起合成一节，此前是两张卡）。

| 选项 | 说明 |
| --- | --- |
| 启用简入繁出 | 打简体、出繁体。候选与上屏均输出繁体，基于 OpenCC 词典转换 |
| 转换变体 | 简入繁出的目标字形与词汇风格，见下 |
| 启用繁入简出 <Since v="0.121" /> | 反方向：**词库和候选本身是繁体，上屏时转成简体**。只有标准一档，没有变体可选 |

| 变体 | 说明 |
| --- | --- |
| 标准繁体 | 通用繁体字形 |
| 台湾繁体 | 台湾标准字形 |
| 台湾繁体（含词汇） | 台湾字形 + 词汇替换（如「软件」→「軟體」） |
| 香港繁体 | 香港标准字形 |

<Callout type="warn" title="两个方向只能开一个">
开一个会自动把另一个关掉。内部候选只有一份，两个方向同时开等于先转过去再转回来，繁体简体都拿不到。
</Callout>

**简入繁出**日常开关用快捷键 `Ctrl + Shift + J`，或[功能主菜单](/docs/start/first-input#context-menu)里的「简入繁出」；变体只能在这里选。

**繁入简出**出厂没有绑快捷键（`keys.toggle_t2s` 默认 `none`），要用请去[按键页 → 功能快捷键](/docs/settings/keys#function-keys)自己配一个。工具栏另有一格「繁→简」，出厂不显示，在[外观页 → 工具栏](/docs/settings/appearance#toolbar)里打开。

## Emoji <Since v="0.121" /> [#emoji]

打出词语后，在候选里追加对应的 emoji（「开心」→ 😄）。**按候选文字查表，与编码无关**，所有方案通用。首次开启时会在本机建立一次索引，稍候片刻。

| 选项 | 默认 | 说明 |
| --- | --- | --- |
| 启用 emoji 候选 | 关 | 总开关。关着时连词表都不加载，不占内存 |
| 显示位置 | 紧随词后 | 「紧随词后」插在对应词之后、其后候选序号顺延；「列表末尾」追加在常用候选之后、原有序号不变 |
| 适用范围 | 仅完整词 | 「仅完整词」只为完整匹配的词追加；「全部候选」补全与部分匹配的候选也追加 |
| 追加范围 | 1 | 只为列表前 N 个候选追加。`1` = 仅首选 |
| 每词上限 | 3 | 一个词最多追加几个 emoji |
| 最少字数 | 2 | 候选少于此字数时不追加，即默认不为单字追加 |
| 启用分类词 | 关 | 「动物」「水果」这类分类词会带出整组 emoji，数量仍受「每词上限」约束 |

三项默认值合起来才是「不打扰」的那套配置：只扩首选、只扩完整词、单字不扩。**觉得候选序号老在漂**，先把「追加范围」调回 1；**觉得出得太少**，把「适用范围」放到「全部候选」。

<Callout type="info" title="选 emoji 不会改变排序">
emoji 不参与词频学习，选多少次它的位置都由上面几项决定。想让某个 emoji 常驻靠前，用[快捷输入](/docs/settings/input/quick-input)或短语。
</Callout>

## Unicode 输入 <Since v="0.121" /> [#unicode]

打出 `u+` 再接十六进制码点，直接得到该字符：`u+4e00` → 一。空格或回车上屏。

| 选项 | 默认 | 说明 |
| --- | --- | --- |
| 启用 Unicode 输入 | 关 | 总开关 |
| 触发前缀 | `u+` `U+` | 识别为码点输入的前缀，按空格分隔。**区分大小写** |
| 候选窗布局 | 跟随全局 | 这段输入期间候选窗的排列方向，退出后自动恢复 |

候选的注释里给出该码点所属的 Unicode 区块名。码点无效时不出候选，此时按空格上屏的是你打的原文。

<Callout type="warn" title="不要把前缀填成单个字母">
填 `u` 会把所有 `u` 起手的编码全夺走，码表的 `u` 码元与拼音的 `u` 都打不出来了。前缀至少两个字符。
</Callout>

## 词频 [#frequency]

| 选项 | 说明 |
| --- | --- |
| 不参与词频的字符 | 勾中的字符不记录词频，也不受已有记录影响 |

三档可勾：**emoji**（表情、旗帜与各类图形符号）、**符号**（标点、数学、箭头、带圈数字、全角形式）、**其它**（新版 Unicode 新增、本程序还不认识的区块）。

emoji 多是一次性点缀，让它参与调频会把常用字挤下去——这是这一项存在的主要理由。配置键是 `schema.frequency.exclude_blocks`，见[词频与候选排序](/docs/guides/frequency)。

## 按键冲突提示

「输入」页顶部可能出现一条**琥珀色警告条**，列出当前的按键冲突（如快捷输入触发键与次选键撞了、临时拼音触发键与临时英文触发键撞了）。它固定在内容区顶部、不随页面滚动，切页也常驻，直到冲突解决为止。

同一个物理键只能分配给一项功能。冲突的完整判定规则见[按键设置 · 冲突与优先级](/docs/settings/keys#冲突与优先级)。
