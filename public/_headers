# Cloudflare Pages 响应头配置。public/ 下的文件会原样拷进 out/。
# 文档：https://developers.cloudflare.com/pages/configuration/headers/
#
# ⚠️ 同一套规则在仓库根的 edgeone.json 里还有一份，两处需同步维护。
#
# 不是重复配置，是**两个平台各认各的格式**：本文件只有 Cloudflare Pages 认，
# edgeone.json 只有 EdgeOne Pages 认。站点迁移期间两种托管形态都可能在跑
# （EdgeOne 回源 CF Pages 时本文件生效；EdgeOne Pages 直接托管时 edgeone.json
# 生效），谁也不能删。改缓存策略时记得两边都改，否则切换托管方式的那一刻
# 缓存行为会悄悄变样。

# Next 构建产物文件名带内容 hash，可以长期强缓存
/_next/static/*
  Cache-Control: public, max-age=31536000, immutable

# 不用 /*.png：通配符会贪婪跨路径段，把上面的 /_next/static/* 也匹配到，而同名
# 响应头在 _headers 里是「拼接」而非「覆盖」，会得出自相矛盾的 Cache-Control。
# 页面里引用的图片都已改为从 src/ 静态导入，产物带内容 hash 走上面那条规则；
# 这里剩下的是 Next 按约定生成、URL 不带 hash 的两类文件。
/icon.png
  Cache-Control: public, max-age=604800

# 各文档页的 OG 卡片（app/og/docs/[...slug]/route.tsx 构建期生成）
/og/*
  Cache-Control: public, max-age=604800

# 搜索索引由构建期生成（api/search/route.ts），随每次部署更新。
#
# 产物是 out/api/search —— **单个无扩展名文件，不是目录**。所以必须写精确路径：
# 原先只写 `/api/search/*` 时这条规则从未命中过（通配符匹配的是目录下的东西）。
# 两条都留着：万一日后 fumadocs 改成生成目录结构，不必再回来补。
/api/search
  Cache-Control: public, max-age=3600, must-revalidate
  Content-Type: application/json; charset=utf-8

/api/search/*
  Cache-Control: public, max-age=3600, must-revalidate

/*
  X-Content-Type-Options: nosniff
  Referrer-Policy: strict-origin-when-cross-origin
  X-Frame-Options: SAMEORIGIN
