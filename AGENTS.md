# AGENTS.md

清风输入法文档站（Next.js + fumadocs，静态导出）。文档正文在 `content/docs/**.mdx`。

## 铁律：写新功能，就得标版本

**凡是描述某个版本才有的功能、配置项、行为或默认值，必须挂 `<Since v="0.117" />` 徽章。**
漏标不会让构建失败，也不会有人当场发现——它只是让这条内容从版本索引里消失。

**为什么**：`src/lib/since-index.ts` 在构建期扫一遍 MDX 源码，把所有 `<Since>` 反查成
「哪个版本对应哪些文档小节」，喂给两处：

- 更新记录页（`/changelog`）每个版本下的「本版相关配置」深链
- 功能聚合页（`/whats-new`）按版本纵览的配置入口

`data/releases.json` 里的更新记录只有一句「增加剪贴板反查功能」，读者看完仍不知道去哪配。
这份对应关系全靠 `<Since>` 建立，漏标一处，那条更新记录就永远落不到文档。

### 标在哪

按能藏住的最小单位标（未发布内容靠整块隐藏，见下）：

- **整节是新功能** —— 标在小节标题：
  `## 联想（input.association） <Since v="0.117" /> [#association]`
- **整页是新功能** —— 标在正文开头，单独一行。整页标了，页内小节就别再各标一遍：
  页面上会挂出一排重复徽章，版本索引里也会拿五个小节把「这一页」那一条淹掉
- **表格里的一项是新增** —— 标在该行第一列的名称后：
  `| 出简让全 <Since v="0.117" /> | 见下 | 全部简码 |`
- **默认值变了** —— 标在默认值那一列，不是名称：
  `| min_syllables | 整数 | 1–6 | 4 <Since v="0.117" /> | 至少几个音节才给词组候选 |`
- **配置项的类型或能力变了** —— 标在变化的那一列（如类型列），或那句话的末尾。
  别标在名称上，那会读成整项都是新增的
- **句中某个具体能力** —— 标在该能力所在短语的末尾，别插进句子中间把话拆断

版本号写**小版本**：`0.117`，不写 `0.117.0`（`since-index.ts` 按 `minorOf()` 归并，补丁号不参与展示）。

### 带 `<Since>` 的标题必须显式声明锚点

```md
### 反查剪贴板里的字 <Since v="0.117" /> [#clip-reverse]
```

fumadocs 拿标题文字生成 id，徽章会在末尾留下空白，得到 `#反查剪贴板里的字-`——既不可读，
又随文案漂移，而且 `since-index.ts` 收不到这一条。`scripts/check-mdx.mjs` 会拦这个错。

锚点用**英文 slug**。中文标题的自动锚点在 URL 里会被百分号编码，分享时变成一长串
`%E5%8F%8D%E6%9F%A5…`，标题措辞一改链接就断。加锚点时记得全站搜一遍旧链接一并改。

### 未发布版本的标注

标了大于 `data/releases.json` 最新版本的 `<Since>`，内容会被
`src/lib/remark-unreleased.ts` 在构建期打标记、CSS 整块藏起来（`?preview=1` 可查看）。
**能藏住的单位只有小节标题、表格行、列表项**——写在普通段落中间藏不掉，`check-mdx` 会报错。

**整页未发布**（页首那行独立标注指向未发布版本）走另一条路：正文之外，页面还有一圈入口，
它们各自从别的数据生成，藏正文那条 CSS 一个都够不到。`src/lib/source.ts` 的
`unreleasedPages` 把这几处一并摘掉——侧栏与 `llms.txt`（剪枝后的页面树）、站点地图、
搜索索引、`llms-full.txt` 与各页 `content.md`，页面本身则连标题与描述一起裹进隐藏容器，
并挂上 `robots: noindex`。**新增这类出口时记得接上这个集合**，漏一个就等于留了条侧门。

页面仍会被静态导出，`?preview=1` 拿链接直接看得到——但侧栏和右侧目录在服务端就定了，
预览模式下也不会出现，别指望从导航走进去。

版本发布后要回头把这些标记改成实际发布的版本号，别只是把标记删掉了事。

## 发版后的核对

`data/releases.json` 更新到新版本后，拿「✨ 新功能」那一段逐条对照：每条要么能在文档里
找到对应的 `<Since v="新版本" />`，要么是内部改动（权重重构一类）不需要文档。
优化改进里改了**出厂默认值**或**配置项写法**的，同样要标。

对照命令：

```bash
node -e "const r=require('./data/releases.json');console.log(r[0].notes.join('\n'))"
grep -rn 'Since v=\"0.117\"' content
```

## 改完要跑

```bash
pnpm lint          # check-mdx（裸尖括号、Since 锚点、未发布标注位置）+ biome
pnpm build         # 静态导出
pnpm lint:links    # 站内链接可达性，必须在 build 之后跑（检查的是 out/）
```

`lint:links` 是加锚点后的必查项——改锚点最容易留下站内坏链。
